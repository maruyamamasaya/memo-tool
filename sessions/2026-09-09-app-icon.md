# Shared Memo app icon

## Request

Shared Memo にアプリを識別できるアイコンを追加する。

## Investigation

既存のロゴ画像と favicon はなく、既定テーマの基調色は `#2563eb` だった。

## Changes

メモ用紙と共有リンクを組み合わせた青い正方形アイコンを生成した。ヘッダーのブランド表示、favicon、Apple touch icon に設定した。

## Validation

Static asset / DOM ID / JavaScript 構文 check、Markdown link check、`git diff --check` を実行し、すべて通過した。実ブラウザでの表示確認は実施していない。

## Result

サイト内とブラウザのタブで Shared Memo を識別できるようになった。

## Remaining Issues

なし。
